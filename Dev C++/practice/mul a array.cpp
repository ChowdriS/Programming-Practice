#include<stdio.h>
#include<string.h>
int main()
{
	/*int n,i,a,j,t;
	printf("-->");
	scanf("%d",&n);
	int m[n];
	printf("-->");
	for (i=0;i<n;i++)
		scanf("%d",&m[i]);
	printf("Before Multiplication:\n");
	for (i=0;i<;i++)
		printf("%d",m[i]);
	printf("Enter the value to be multiplied : ");
	scanf("%d",&a);
	for (i=0;i<n;i++)
		m[i]=m[i]*a;
	printf("After multiplication:\n");
	for (i=0;i<n;i++)
		printf("%d ",m[i]);
	for (i=0;i<n;i++)
	{
		for (j=i+1;j<n;j++)
		{
			if (m[i]<m[j])
			{
				t=m[i];m[i]=m[j];m[j]=t;
			}
		}
	}
	printf("\n");
	for (i=0;i<n;i++)
		printf("%d ",m[i]);*/
	/*int a=0,b=1;
	a=a+b;
	b=a-b;
	a=a-b;
	printf("%d %d",a,b);
	char a[100],ref;
	int i,j,count=0;
	scanf("%s",a);
	int n=strlen(a);
	for (i=0;i<n;i++)
	{
		for (j=i+1;j<n;j++)
		{
			if (a[i]==a[j])
			{
				printf("hi");
				ref=a[i];
				++count;
			}
		}
	}
	printf("%d times %c",count+1,ref);*/
}
